describe("Sample Test Group", () => {
  beforeAll(() => {});

  beforeEach(() => {});

  test("test 1", () => {
    expect(1 + 1).toBe(2);
  });

  test("test 2", () => {
    expect(2 + 2).toBe(4);
  });

  afterEach(() => {});

  afterAll(() => {});
});
